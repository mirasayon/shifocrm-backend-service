info:
  name: Delete Visit Service By Visit And Tooth
  type: http
  seq: 4
http:
  method: DELETE
  url: '{{baseUrl}}/api/visit-services/by-visit/{{visitId}}/tooth/{{toothId}}'
  auth:
    type: bearer
    token: '{{token}}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
runtime:
  scripts:
    - type: tests
      code: |-
        test("status should be 200", function () {
          expect(res.status).to.equal(200);
        });
