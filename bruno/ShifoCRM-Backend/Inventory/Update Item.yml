info:
  name: Update Item
  type: http
  seq: 4
http:
  method: PATCH
  url: '{{baseUrl}}/api/inventory/items/{{inventoryItemId}}'
  auth:
    type: bearer
    token: '{{token}}'
  body:
    type: json
    data: |
      {
        "quantity": 20,
        "description": "Updated description"
      }
