info:
    name: Delete Consumption
    type: http
    seq: 13

http:
    method: DELETE
    url: "{{baseUrl}}/api/inventory/consumptions/{{consumptionId}}"
    auth: bearer

auth:
    type: bearer
    bearer:
        token: "{{token}}"

