info:
    name: Delete Item
    type: http
    seq: 5

http:
    method: DELETE
    url: "{{baseUrl}}/api/inventory/items/{{inventoryItemId}}"
    auth: bearer

auth:
    type: bearer
    bearer:
        token: "{{token}}"

