info:
    name: Delete Movement
    type: http
    seq: 8

http:
    method: DELETE
    url: "{{baseUrl}}/api/inventory/movements/{{movementId}}"
    auth: bearer

auth:
    type: bearer
    bearer:
        token: "{{token}}"

