info:
    name: Get Inventory Items
    type: http
    seq: 1

http:
    method: GET
    url: "{{baseUrl}}/api/inventory/items"
    auth: bearer

auth:
    type: bearer
    bearer:
        token: "{{token}}"
