info:
    name: Get Movements
    type: http
    seq: 6

http:
    method: GET
    url: "{{baseUrl}}/api/inventory/movements"
    auth: bearer

auth:
    type: bearer
    bearer:
        token: "{{token}}"

