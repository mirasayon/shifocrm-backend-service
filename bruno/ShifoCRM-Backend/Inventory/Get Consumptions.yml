info:
    name: Get Consumptions
    type: http
    seq: 12

http:
    method: GET
    url: "{{baseUrl}}/api/inventory/consumptions?visitId={{visitId}}"
    auth: bearer

auth:
    type: bearer
    bearer:
        token: "{{token}}"

