info:
    name: Delete Visit Service By Id
    type: http
    seq: 3

http:
    method: DELETE
    url: "{{baseUrl}}/api/visit-services/{{visitServiceId}}"
    auth: bearer

auth:
    type: bearer
    bearer:
        token: "{{token}}"

