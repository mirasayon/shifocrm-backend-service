info:
    name: Get Visit Services
    type: http
    seq: 1

http:
    method: GET
    url: "{{baseUrl}}/api/visit-services?visitId={{visitId}}"
    auth: bearer

auth:
    type: bearer
    bearer:
        token: "{{token}}"

