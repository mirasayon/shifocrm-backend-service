info:
    name: Get Odontogram By Visit
    type: http
    seq: 1

http:
    method: GET
    url: "{{baseUrl}}/api/odontogram/by-visit/{{visitId}}"
    auth: bearer

auth:
    type: bearer
    bearer:
        token: "{{token}}"

