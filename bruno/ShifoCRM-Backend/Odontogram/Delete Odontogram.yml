info:
    name: Delete Odontogram
    type: http
    seq: 7

http:
    method: DELETE
    url: "{{baseUrl}}/api/odontogram/{{odontogramId}}"
    auth: bearer

auth:
    type: bearer
    bearer:
        token: "{{token}}"

