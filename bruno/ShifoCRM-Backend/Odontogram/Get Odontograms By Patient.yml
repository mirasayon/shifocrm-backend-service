info:
    name: Get Odontograms By Patient
    type: http
    seq: 2

http:
    method: GET
    url: "{{baseUrl}}/api/odontogram/by-patient/{{patientId}}"
    auth: bearer

auth:
    type: bearer
    bearer:
        token: "{{token}}"

