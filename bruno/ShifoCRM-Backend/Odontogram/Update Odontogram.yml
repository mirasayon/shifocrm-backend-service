info:
  name: Update Odontogram
  type: http
  seq: 6
http:
  method: PATCH
  url: '{{baseUrl}}/api/odontogram/{{odontogramId}}'
  auth:
    type: bearer
    token: '{{token}}'
  body:
    type: json
    data: |
      {
        "data": {
          "teeth": {
            "11": { "status": "treated" }
          }
        }
      }
