info:
    name: Update Odontogram
    type: http
    seq: 6

http:
    method: PATCH
    url: "{{baseUrl}}/api/odontogram/{{odontogramId}}"
    auth: bearer

auth:
    type: bearer
    bearer:
        token: "{{token}}"

body:
    type: json
    json: |
        {
          "data": {
            "teeth": {
              "11": { "status": "treated" }
            }
          }
        }

