info:
  name: Update Odontogram
  type: http
  seq: 6
http:
  method: PATCH
  url: '{{baseUrl}}/api/odontogram/{{odontogramId}}'
  auth:
    type: bearer
    token: '{{token}}'
  body:
    type: json
    data: |
      {
        "data": {
          "teeth": {
            "11": { "status": "treated" }
          }
        }
      }
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
runtime:
  scripts:
    - type: tests
      code: |-
        test("status should be 200", function () {
          expect(res.status).to.equal(200);
        });
