info:
    name: Create Odontogram
    type: http
    seq: 4

http:
    method: POST
    url: "{{baseUrl}}/api/odontogram"
    auth: bearer

auth:
    type: bearer
    bearer:
        token: "{{token}}"

body:
    type: json
    json: |
        {
          "patientId": {{patientId}},
          "visitId": {{visitId}},
          "data": {
            "teeth": {}
          }
        }

