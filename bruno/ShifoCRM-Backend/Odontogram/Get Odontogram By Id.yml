info:
    name: Get Odontogram By Id
    type: http
    seq: 3

http:
    method: GET
    url: "{{baseUrl}}/api/odontogram/{{odontogramId}}"
    auth: bearer

auth:
    type: bearer
    bearer:
        token: "{{token}}"

