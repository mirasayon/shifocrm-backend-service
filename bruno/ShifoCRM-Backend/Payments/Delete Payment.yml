info:
    name: Delete Payment
    type: http
    seq: 4

http:
    method: DELETE
    url: "{{baseUrl}}/api/payments/{{paymentId}}"
    auth: bearer

auth:
    type: bearer
    bearer:
        token: "{{token}}"

