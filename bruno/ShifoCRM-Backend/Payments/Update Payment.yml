info:
  name: Update Payment
  type: http
  seq: 3
http:
  method: PATCH
  url: '{{baseUrl}}/api/payments/{{paymentId}}'
  auth:
    type: bearer
    token: '{{token}}'
  body:
    type: json
    data: |
      {
        "amount": 60000,
        "note": "Updated payment amount"
      }
