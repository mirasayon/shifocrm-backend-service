info:
    name: Update Payment
    type: http
    seq: 3

http:
    method: PATCH
    url: "{{baseUrl}}/api/payments/{{paymentId}}"
    auth: bearer

auth:
    type: bearer
    bearer:
        token: "{{token}}"

body:
    type: json
    json: |
        {
          "amount": 60000,
          "note": "Updated payment amount"
        }

