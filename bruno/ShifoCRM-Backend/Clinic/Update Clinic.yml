info:
    name: Update Clinic
    type: http
    seq: 2

http:
    method: PUT
    url: "{{baseUrl}}/api/clinic"
    auth: bearer

auth:
    type: bearer
    bearer:
        token: "{{token}}"

body:
    type: json
    json: |
        {
          "name": "Shifo Test Clinic",
          "logoUrl": "https://example.com/logo.png",
          "maxDoctors": 5,
          "isActive": true
        }

