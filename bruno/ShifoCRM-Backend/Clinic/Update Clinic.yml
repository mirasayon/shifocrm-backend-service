info:
  name: Update Clinic
  type: http
  seq: 2
http:
  method: PUT
  url: '{{baseUrl}}/api/clinic'
  auth:
    type: bearer
    token: '{{token}}'
  body:
    type: json
    data: |
      {
        "name": "Shifo Test Clinic",
        "logoUrl": "https://example.com/logo.png",
        "maxDoctors": 5,
        "isActive": true
      }
