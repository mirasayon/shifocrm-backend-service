info:
    name: Get Visit By Id
    type: http
    seq: 4

http:
    method: GET
    url: "{{baseUrl}}/api/visits/{{visitId}}"
    auth: bearer

auth:
    type: bearer
    bearer:
        token: "{{token}}"

