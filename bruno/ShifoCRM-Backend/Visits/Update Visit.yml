info:
  name: Update Visit
  type: http
  seq: 3
http:
  method: PATCH
  url: '{{baseUrl}}/api/visits/{{visitId}}'
  auth:
    type: bearer
    token: '{{token}}'
  body:
    type: json
    data: |
      {
        "status": "COMPLETED_PAID",
        "paidAmount": 500000
      }
