info:
    name: Update Visit
    type: http
    seq: 3

http:
    method: PATCH
    url: "{{baseUrl}}/api/visits/1"
    auth: bearer

auth:
    type: bearer
    bearer:
        token: "{{token}}"

body:
    type: json
    json: |
        {
          "status": "COMPLETED_PAID",
          "paidAmount": 500000
        }
