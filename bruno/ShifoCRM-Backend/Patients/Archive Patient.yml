info:
    name: Archive Patient
    type: http
    seq: 5

http:
    method: DELETE
    url: "{{baseUrl}}/api/patients/{{patientId}}"
    auth: bearer

auth:
    type: bearer
    bearer:
        token: "{{token}}"

