info:
    name: Get Patient By Id
    type: http
    seq: 3

http:
    method: GET
    url: "{{baseUrl}}/api/patients/{{patientId}}"
    auth: bearer

auth:
    type: bearer
    bearer:
        token: "{{token}}"

