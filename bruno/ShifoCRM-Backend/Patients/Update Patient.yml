info:
  name: Update Patient
  type: http
  seq: 4
http:
  method: PATCH
  url: '{{baseUrl}}/api/patients/{{patientId}}'
  auth:
    type: bearer
    token: '{{token}}'
  body:
    type: json
    data: |
      {
        "notes": "Updated notes",
        "status": "active",
        "doctorId": {{doctorId}}
      }
