info:
    name: Update Patient
    type: http
    seq: 4

http:
    method: PATCH
    url: "{{baseUrl}}/api/patients/{{patientId}}"
    auth: bearer

auth:
    type: bearer
    bearer:
        token: "{{token}}"

body:
    type: json
    json: |
        {
          "notes": "Updated notes",
          "status": "active",
          "doctorId": {{doctorId}}
        }

