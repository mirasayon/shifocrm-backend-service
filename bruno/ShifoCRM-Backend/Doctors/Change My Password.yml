info:
  name: Change My Password
  type: http
  seq: 4
http:
  method: PATCH
  url: '{{baseUrl}}/api/doctors/me/password'
  auth:
    type: bearer
    token: '{{token}}'
  body:
    type: json
    data: |
      {
        "currentPassword": "{{adminPassword}}",
        "newPassword": "newStrongPassword123"
      }
