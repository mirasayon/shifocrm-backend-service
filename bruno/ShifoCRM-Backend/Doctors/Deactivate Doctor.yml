info:
    name: Deactivate Doctor
    type: http
    seq: 8

http:
    method: DELETE
    url: "{{baseUrl}}/api/doctors/{{doctorId}}"
    auth: bearer

auth:
    type: bearer
    bearer:
        token: "{{token}}"

