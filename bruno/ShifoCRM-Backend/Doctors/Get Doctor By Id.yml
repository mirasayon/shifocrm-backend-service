info:
    name: Get Doctor By Id
    type: http
    seq: 6

http:
    method: GET
    url: "{{baseUrl}}/api/doctors/{{doctorId}}"
    auth: bearer

auth:
    type: bearer
    bearer:
        token: "{{token}}"

