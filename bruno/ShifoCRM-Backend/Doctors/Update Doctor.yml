info:
  name: Update Doctor
  type: http
  seq: 7
http:
  method: PATCH
  url: '{{baseUrl}}/api/doctors/{{doctorId}}'
  auth:
    type: bearer
    token: '{{token}}'
  body:
    type: json
    data: |
      {
        "fullName": "Dr. Jane Doe (Updated)",
        "specialization": "Orthodontist",
        "isActive": true
      }
